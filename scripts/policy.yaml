id: script_cache_policy_v1
default:
  strategy: fingerprint
  max_age_seconds: null
overrides:
  - check: crate-dependency-graph
    strategy: fingerprint
  - check: unsafe-code-scan
    strategy: always_rerun
  - check: module-boundary-diff
    strategy: fingerprint
  - check: cargo-fmt
    strategy: fingerprint
